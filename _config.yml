# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
lang: en-US
title: Anson Liu
email: anson@ansonliu.com
description: Software design, 3D printing, and outdoor adventures
baseurl: ""
url: "https://ansonliu.com"
author:
  name: Anson Liu
  url: https://ansonliu.com
  picture: https://avatars1.githubusercontent.com/ansonl?v=3&s=200
social:
  links:
    - https://github.com/ansonl
    - https://www.printables.com/@ansonl
    - https://www.linkedin.com/in/console/
    - https://www.facebook.com/ansonl
    - https://www.youtube.com/@maketheuncharted
    - https://www.instagram.com/maketheuncharted/
github_username: ansonl
printables_username: ansonl
makerworld_username: ansonl
linkedin_username: console
youtube_username: maketheuncharted
instagram_username: maketheuncharted
logo: # path of site logo, e.g. "/assets/images/logo.png"
search: true
search_full_content: false # true, false (default)
search_provider: lunr

#Custom settings
permalink: /:year/:month/:title/
# No analytics. The previous UA-55429613-1 property was Universal Analytics,
# which stopped collecting data in July 2023. Re-adding means a GA4 property
# and a fresh consent implementation.
disqus:
  shortname: apparentetch

# Newsletter signup (_includes/subscribe.html). Unset form_action to hide the form.
# Get the URL from Brevo: Contacts -> Forms -> Share -> the iframe src.
brevo:
  form_action: "https://65040f50.sibforms.com/serve/MUIFANPtYpdkDD-P1iGVw3TCuizWnR9kGkk8Nq27UG85SlSTf7PHxXLzozgG9at2TQ876HeILP3SP9llunZ6uue_pSG5GPtDSKTKaQKggiwHmunD5UhASAmFsk6U5px2TKa3ZR64L3IJfzvuQ-blmoFQAZv47AM60rAfrpn-wYBalwvGcvkCCoFCRUXJPc_C7IgbA3rkLPxs1w-A"
  heading: "Subscribe"
  blurb: "New posts on software design, 3D printing, and outdoor adventures."

# Build settings
markdown: kramdown
highlighter: rouge

#kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
  auto_ids: true
  # These were injected by the github-pages gem and are NOT Jekyll/kramdown
  # defaults. kramdown-parser-gfm defaults hard_wrap to TRUE, which would turn
  # every intra-paragraph newline across 188 posts into <br />.
  hard_wrap: false
  syntax_highlighter_opts:
    default_lang: plaintext

# Also previously supplied by github-pages. Left true (the jekyll-gist default),
# each of the 9 {% gist %} tags makes a build-time GitHub API call whose failure
# path silently drops the content.
gist:
  noscript: false

# github-pages forced this under JEKYLL_ENV=production. Dart Sass would
# otherwise default to expanded output plus a source map.
sass:
  style: compressed
  # Dart Sass emits a source map by default; we do not want a 74KB .map shipped.
  sourcemap: never
  # The upstream theme's SCSS uses lighten()/darken() and slash division, both
  # deprecated in Dart Sass. It is a remote dependency we do not control, and
  # the warnings are pure noise on every build.
  quiet_deps: true

# github-pages set this; Jekyll's own default is false, which would hide any
# post dated ahead of build time.
future: true

# GitHub Pages builds in UTC; without this a local build emits your own offset
# and can never be diffed against production.
timezone: UTC

# Jekyll 4 APPENDS its DEFAULT_EXCLUDES (Gemfile, Gemfile.lock, node_modules,
# vendor/*, .sass-cache, .jekyll-cache, gemfiles) to whatever is listed here, so
# only site-specific entries belong below. Jekyll 3 replaced them instead, which
# is why this list used to restate all seven.
exclude:
  - CNAME
  # Dev scripts; these were being served from the live site.
  - jekyllServe.sh
  - jekyllNginxServe.sh
  - rename-loop.sh

plugins:
  - jekyll-remote-theme
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate
  - jekyll-gist

feed:
  path: atom.xml

# Pagination - https://jekyllrb.com/docs/pagination/
paginate: 5
paginate_path: /page:num/

# Collections
collections:
  maps:
    output: true
    permalink: /:collection/:path/
  store:
    output: true
    permalink: /:collection/:path/
  portfolio:
    output: true
    permalink: /:collection/:path/


# Front Matter Defaults
defaults:
  # _posts
  - scope:
      path: "_posts"
      type: posts
    values:
      layout: post
      read_time: true
  - scope:
      path: "_drafts"
      type: drafts
    values:
      layout: post
      read_time: true

# Theme files come from upstream; only the handful in _includes/, _layouts/ and
# _sass/ in this repo override it. Do not set `theme:` as well -- remote_theme
# supplies it, and leaving it unset lets the github-pages gem default to
# jekyll-theme-primer, which silently ships its own stylesheet.
remote_theme: "mmistakes/jekyll-theme-basically-basic"